# Advance-Parking-Automation
A multi-level parking system supporting 30 slots across basements, with VIP priority parking. Features include real-time parking/exit with timestamps/billing, hover tooltips for slot details, and queueing when full. Backend exposes C functions via shared library (e.g., parking.dll/so) for FFI calls from Python.
